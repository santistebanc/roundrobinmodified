        <div class="{{divclass}}">
            <a href="{{ref}}">
                <h5 class="{{boxcolor}} box align-center">
                    <div class="gap-top gap-bottom"><img src="{{src}}" class="{{shape}}">
                        <div class="small {{textbg}} button">{{text}}</div>
                    </div>
                </h5>
            </a>
        </div>